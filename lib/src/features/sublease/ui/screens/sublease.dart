import 'package:flutter/material.dart';

class Sublease extends StatelessWidget {
  const Sublease({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: const Text('Sublease'),
      // ),
      body: const Center(
        child: Text('This is the Sublease screen'),
      ),
    );
  }
}